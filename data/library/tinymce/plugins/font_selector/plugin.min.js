tinymce.PluginManager.add("vnp_image", function (t) {
	
    t.addButton("vnp_image", {
        icon: "browse",
        tooltip: "Insert/edit image",
        onclick: function() {
			
            // Open window
            t.windowManager.open({
                title: 'VNP Media management',
				width: 610,
                height: 500,
				url: base + '/admin.php?module=media&op=editor_medias&ajax=string',
                onsubmit: function(e) {
					alert('ok');
                    // Insert content when the window form is submitted
                    t.insertContent('Title: ' + e.data.title);
                }
            });
        },
        stateSelector: "img:not([data-mce-object])"
    })
	
	
	// Adds a menu item to the tools menu
    t.addMenuItem('vnp_image', {
        text: 'Add image',
        context: 'tools',
        onclick: function() {
            // Open window with a specific url
            t.windowManager.open({
                title: 'VNP Media management',
                url: base + '/admin.php?module=media&op=editor_medias&ajax=string',
                width: 610,
                height: 500,
                buttons: [{
                    text: 'Close',
                    onclick: 'close'
                }]
            });
        }
    });
});